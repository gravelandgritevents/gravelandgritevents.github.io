# Source assets

This directory contains recoverable, full-resolution source material that is not
published with the website. It is explicitly excluded from the site build in
`_config.yml`.

Use the optimised files in `images/` for production pages. If an original is
needed for a future edit, create a suitably resized and compressed derivative in
`images/` rather than linking directly to the original.
